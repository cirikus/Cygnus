<br>
<div>  
  <%= link_to "Back", tasks_path, class: "btn btn-default btn-xs" %>
  <% if admin_signed_in? %>
      <%= link_to "Edit", edit_task_path(@task), class: "btn btn-default btn-xs" %>
      <%= link_to "Delete", task_path(@task), class: "btn btn-danger btn-xs", data: {:confirm => "Are you sure?"}, method: :delete %>
  <% end %>
</div>