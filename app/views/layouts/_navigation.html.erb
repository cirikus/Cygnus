<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Cygnus", root_path , class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="glyphicon glyphicon-tasks"></i>&nbsp;Tasks<b class="caret"></b> </a>
          <ul class="dropdown-menu">
            <li><%= link_to "<i class='glyphicon glyphicon-list'></i>&nbsp; List".html_safe, tasks_path %></li>
              <% if admin_signed_in? %>
                <li><%= link_to "<i class='glyphicon glyphicon-plus'></i>&nbsp; New".html_safe, new_task_path %></li>
              <% end %>
          </ul>
        </li>
        <% if admin_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="glyphicon glyphicon-user"></i>&nbsp;<%= current_admin.first_name %>&nbsp;<%= current_admin.last_name %><b class="caret"></b> </a>
            <ul class="dropdown-menu">
              <li><%= link_to "<i class='glyphicon glyphicon-cog'></i>&nbsp; Settings Profile".html_safe,  edit_admin_registration_path  %></li>
              <li><%= link_to "<i class='glyphicon glyphicon-log-out'></i>&nbsp; Logout".html_safe, destroy_admin_session_path, :method => :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "<i class='glyphicon glyphicon-log-in'></i>&nbsp; Login".html_safe, new_admin_session_path  %></li>  
        <% end %>
      </ul>
    </div>
  </div>
</nav>